export interface Parametros{
      fechaini ?: string;
      fechafin ?: string;
      usuario ?: string;
      contrasena ?: string;
      empresa ?: string;
      campana ?: number;
      tipoDoc?: string;
      nroDoc?: string;
      nroCliente?: string;
  }
  //? cuando llame la interface se opcional