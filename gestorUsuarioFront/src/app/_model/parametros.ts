export interface Parametros{
      fechaini ?: string;
      fechafin ?: string;
      usuario ?: string;
      contrasena ?: string;
      empresa ?: string;
      campana ?: number;
      tipoDoc?: string;
      nroDoc?: string;
      nroCliente?: number;
  }
  //? cuando llame la interface se opcional